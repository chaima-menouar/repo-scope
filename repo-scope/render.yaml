services:
  - type: web
    name: repo-scope
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: OPENAI_API_KEY
        sync: false
